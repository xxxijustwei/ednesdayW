---
title: Select
description: Displays a list of options for the user to pick from—triggered by a button.
---

### Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add "https://ui.ednesdayw.com/r/select.json"
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project's `components/ui/select.tsx` file</Step>

<ComponentSource name="select" />

</Steps>

</TabsContent>

</Tabs>

### Usage

```tsx
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";

const Page = () => {
  return (
    <Select>
      <SelectTrigger className="w-[180px]">
        <SelectValue placeholder="Theme" />
      </SelectTrigger>
      <SelectContent>
        <SelectItem value="light">Light</SelectItem>
        <SelectItem value="dark">Dark</SelectItem>
        <SelectItem value="system">System</SelectItem>
      </SelectContent>
    </Select>
  );
};
```

### Example

#### Size

<ComponentPreview name="select-size-demo" />

#### Variant

<ComponentPreview name="select-variant-demo" />

#### Disabled

<ComponentPreview name="select-disabled-demo" />

#### Invalid

<ComponentPreview name="select-invalid-demo" />

#### Start Content

<ComponentPreview name="select-start-content-demo" />

#### Clearable

<ComponentPreview name="select-clearable-demo" />
