---
title: Pagination
description: Displays a pagination component.
---

### Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add "https://ui.ednesdayw.com/r/pagination.json"
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the `paginate` library.</Step>

```bash
npx shadcn@latest add "https://ui.ednesdayw.com/r/paginate.json"
```

<Step>Copy and paste the following code into your project's `components/ui/pagination.tsx` file</Step>

<ComponentSource name="pagination" />

</Steps>

</TabsContent>

</Tabs>

### Usage

```tsx
import { Pagination } from "@/components/ui/pagination";

const Page = () => {
  return (
    <Pagination currentPage={1} totalPages={10} onChange={setCurrentPage} />
  );
};
```

### Example

<ComponentPreview name="pagination-demo" />
